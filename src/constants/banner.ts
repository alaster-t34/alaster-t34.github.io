// Visual banner sizing overrides for the customized theme.
// Kept separate from constants.ts to avoid conflicts with upstream theme defaults.
export const BANNER_VISUAL_HEIGHT = 40
export const BANNER_VISUAL_HEIGHT_EXTEND = 24
export const BANNER_VISUAL_HEIGHT_HOME =
  BANNER_VISUAL_HEIGHT + BANNER_VISUAL_HEIGHT_EXTEND
